# 微盘历史研究 checkpoint 1 — 2026-09-08

> 最新进展：见末尾Checkpoint 7。2026-09-09用户明确授权使用后期快照并集进行简化历史估算，新增独立研究路径；不再用严格Gate阻塞该估算路径。原严格PIT路径及其Unknown状态保留。

状态：来源审计和旧路径保护已实施；PIT 重建未完成。Gate 1A = Unknown；Gate 1B = Unknown。禁止向无真实验证区间批量生成排名。

## 已确认事实（Fact）

- 既有快照文件包含 2026-06-22 至 2026-09-07 的 56 个日期、22400 行。
- 2026-06-24、07-03、08-10、08-12、08-18、08-21 共 2400 行，按证券代码排序后的代码、价格、市值与旧六日重建文件逐条相同。不能将这些记录作为独立 observed 验证基准。
- 尚未匹配到旧重建的其他日期，也不能仅据此证明是真实采集。审计保留 Unknown，等待采集日志或其他独立证据；没有删除或改写输入。
- TickFlow instruments 元数据请求只有 symbols 参数，不能证明 historical effective shares。参考：[官方接口](https://docs.tickflow.org/zh-hans/api-reference/标的/查询标的元数据)。
- 巨潮 `stock_share_change_cninfo` 对 002594、2025-01-01 至 2026-09-08 的只读探针返回 5 行，包含公告日期、变动日期、总股本、变动原因。含定期报告记录；有公告日期晚于变动日期的记录，不能直接 as-of join 后用于当时决策。参考：[AKShare 接口文档](https://akshare.akfamily.xyz/data/stock/stock.html)。
- 上交所 `stock_info_sh_delist` 只读探针返回 159 行，字段为公司代码、公司简称、上市日期、暂停上市日期。暂停上市日期不能未经验证改名为退市生效日。
- 首次沙箱请求因本地代理连接失败；受控网络重试成功。不能将首次连接错误解释成数据不存在。

输入审计 SHA256：

| 输入 | SHA256 |
|---|---|
| 既有快照 CSV | `01018dc0c7f2b23355dd930a4838484917dc2ae83a3bb33bec673be35c73c00f` |
| 六日旧重建 CSV | `ae9d8fd95dc3abb87872d2f671f72b870185521be9e355861258cb89c1b4c0c4` |

审计工具直接读取文件，不初始化数据库；报告仅允许新建文件，已有路径拒绝覆盖。独立 JSON 报告本次保存在 `/tmp/microcap-research-checkpoint-1.json`。可复现入口：`scripts/audit_microcap_research_inputs.py --snapshots <CSV> --legacy <旧重建CSV> [--report <新JSON>]`。

## 已实施保护

- 旧六日重建与历史扩展入口在任何网络、目录创建或数据写入之前拒绝执行。旧算法暂留供存量产物审计，不再可运行。
- 快照指标默认不加载历史扩展；显式要求拼接也会拒绝。
- 存量页面与指标标签改为来源待核验，不再声称所有历史行均为真实快照。原始数据保留。

## 已锁定的研究设计（Assumption）

- `BK1158_observed` 和 `microcap_rule_reconstructed` 永久分离；沪深全A股 PIT universe 必须逐日完整，包括后来退市者，不能用任何成分并集替代。
- `selection_date`、`market_data_asof=EOD`、`information_available_at`、`effective_date` 分开保存。正式EOD价格只供下一交易日及以后使用；数据获取时间另存，不能捏造历史可用时间。
- 同时保留 `raw_market_cap_rank` 与 `strategy_rank`，独立记录 `index_member`、`rule_pool_member`、`strategy_eligible`、`tradable`。ST、停牌不删除原始市值排名或 observed 成员事实。
- 股本计算只接受当时可知且实际生效的总股本。发布较晚的回溯资料可以核验事实，不可提前用于历史决策。
- 下一交易日09:25复核按已公开且生效事件过滤，按此前EOD固定排名候补；复核后新增限制仅阻止执行，不重排。
- Gate 1A 验证 PIT 数据及研究池正确性，包含至少20只股本变动股票的独立对照。Gate 1B 单独验证 BK1158 复现程度；1B失败不否定1A，不以重合率修改研究规则。
- 正式重合率前先测试 observed T 对 reconstructed T 和上一交易日的有效时点关系。不能把最优重合日移自动当作生效机制的证明；需独立来源证据，无法确定则 Unknown。
- 指标包含 Top20 exact overlap、Jaccard、rank MAE、market-cap MAPE、15～25名边界表，Top50/200/400 overlap，missing eligible securities 和 uncertain boundary securities。

## 推论（Inference）及未决项（Unknown）

- Inference：六日数据的完全匹配与旧算法写入路径支持“可能已混入旧估算”；没有写入日志前不声称已查清写入过程。
- Unknown：其他50个日期的采集证据、快照时点、正式收盘对应关系。
- Unknown：完整沪深历史证券生命周期、独立完整性对照、股本单位和实际生效语义、全部ST/停复牌事件的历史可用时间。
- Unknown：BK1158是否取最小400只、停牌及首发连板处理、计算和生效时点、与万得规则的关系。全部是待检验假设。

## 下一 checkpoint 的必需证据

1. 为来源建立只读适配及原始证据存档，验证股本事件单位、时间字段、修订和定期报告的区别；不得将此探针当作来源验收。
2. 重建全市场历史生命周期，并以独立历史名录验证完整性；无法证明的日子标 incomplete。
3. 追溯既有快照采集证据，建立可用验证日期清单。未经证明的日期不进入20只股本测试的独立对照。
4. 完成排名、时间边界和两个Gate实现及回归测试，再报告是否允许历史扩展。

这些是未完成工作，不是已通过的Gate。未生成历史研究排名，未进行策略优化，未提交或推送Git。

# Checkpoint 2：来源可行性抽样

## Fact

- 新增 `probe_microcap_pit_sources.py`，从既有快照代码中以固定种子20260908抽取20只做接口可行性检查。此列表仅用于验证来源，绝非历史 universe，也不用于生成排名。
- 20个股本请求均返回数据，共96行；其中60行为定期报告，86行公告日期晚于变动日期。只有9只出现非定期报告记录，而且限售解禁不一定改变总股本。因此不能将本次探针记为“至少20只股本变化验证通过”。
- 上交所退市接口返回159行，深交所返回208行。仅取得两个退市列表，不构成完整历史沪深A股 universe 的证明。
- 本地成功采集日志涉及46个日期，其中45个日期与当前快照文件有交集。日志未附每行数据或哈希，不能证明当前文件行自采集后未变。
- 已保存22组适配器输出、请求参数、检索时间、SHA256及最终报告，位于 `output/microcap_research_evidence_20260908_checkpoint2/`。这些是独立研究产物，不是正式数据，也不是原始HTTP响应。
- 适配器输出 `information_available_at=null`、`pit_approved=false`；检索时间不会填充为历史公开时间。来源单位、生效语义、完整性仍为Unknown。
- 本次探针前后既有快照SHA256相同。

## Inference / Unknown

- Inference：未经原始公告校验的股本表无法直接用于当时排名；按变动日期回填晚公告值有前视风险，按公告日期延迟使用旧值又可能不符合实际生效总股本。两者都不能静默替代。
- Unknown：逐日实际总股本能否由当时公开信息完整复原，尤其可转债转股、期权行权等只按期汇总的情形。
- Unknown：独立验证样本、完整市场名录、全量ST/停牌事件仍缺证据。Gate 1A和1B保持Unknown，历史扩展关闭。

下一步须核对具体原始公告和证券生命周期证据，建立明确的事件接受/拒绝规范及时间模型；对无法证明的区间记录incomplete，不降低标准。

## 本阶段测试

16项专项测试通过，包括迟报及定期报告不升级为PIT事实、检索时间与历史公开时间分离、证据拒绝覆盖、路径穿越拒绝，以及上阶段来源审计和BK1158图表日线回归。测试验证代码行为，不代表真实数据Gate通过。

# Checkpoint 3：PIT时间与独立排名计算

- 新增纯计算模块 `services/microcap_pit.py`，没有联网、缓存、数据库或交易写入路径。
- 决策时间要求带时区、使用显式有证据的交易日历；EOD结果只能供后续交易日使用。保留 selection_date、market_data_asof、information_available_at、effective_date。
- 历史股本必须处在已核验的有效区间内、有当时公开证据及覆盖证据；不无限前填，冲突返回Unknown。
- 当日输入必须与有独立核对证据的完整历史名录逐证券一致；即使仅缺一个大市值股票，也不得认证Top20。
- 保留原始市值排名及策略排名，ST或停牌过滤不删除原始排名和成员事实。拒绝复权价、错误日期及未来价格/股本；未来资格信息不提前使用。
- 次日09:25复核只接收资格事件，没有次日价格输入；按原始固定排名补位。缺乏完整事件覆盖时不输出可执行名单；09:25以后公开事件不改变已冻结复核结果。
- 测试发现并修复了复核表索引与证券列同名造成的排序歧义。27项专项测试全部通过，项目编译和diff检查通过。

输入的Fact及证据引用必须由上游来源验证层裁定，不能把手工布尔值或合成测试当作真实来源证明。现阶段仅以合成边界数据验证算法，尚未接入真实排名生成流程。Gate 1A/1B仍未通过；独立Gate判定、effective-date alignment和全套对照指标尚待实现，真实数据来源缺口仍待补齐。

# Checkpoint 4：独立Gate、对齐诊断及指标

- 新增 `services/microcap_validation.py`：两个Gate独立汇总，证据必须覆盖指定验证日期，缺项或空证据保持Unknown，股本样本不足20只不能通过1A。1B失败不覆盖1A结果；历史扩展仅由1A控制。
- 日期诊断分别测试T和前一实际交易日，不将更高重合率自动升级为Fact。正式对照要求来源证据、已测试两个日期和独立对齐证据；否则拒绝。
- 实现Top20交集数量/比例、exact match、Jaccard、共同股票rank MAE及样本数、同估值时点MAPE、15～25名边界并集，以及Top50/200/400重合与缺失/不确定证券清单。任一TopN排名不完整时相应指标为Unknown。
- 36项专项测试、项目编译和diff检查通过。实际Gate仍为Unknown：没有将合成测试结果写成真实数据通过证据。

## 关键来源边界的公告验证

Fact：[恒锋信息2026年第一季度转股公告](https://static.cninfo.com.cn/finalpage/2026-04-01/1225069966.PDF)于4月1日披露，正文报告该季度累计转股4501股，列示季度末股本，未提供4501股按交易日拆分的变动明细。巨潮适配器对应记录为公告日期4月1日、变动日期3月31日。

Inference：这条“变动日期3月31日”不能证明全部转股在3月31日生效，也不能证明季度内每日股本。仅凭该公告或对应接口行，不可回填当季逐日精确股本。已停止把这一类记录提升为有效股本区间，仍可将其用于事后端点核验。

Unknown：是否存在可取得且具历史可用时间证据的逐日登记/交易所股本源。需要继续检查独立来源；找不到时该区间必须incomplete，不能采用季度末股本前填或当前股本回推。完整历史全市场名录、20只实际股本验证、BK1158生效时点及真实历史排名仍未完成。

# Checkpoint 5：连接恢复后的复核与来源阻塞

工作区连接已恢复，研究文件仍存在。本次实际重跑39项专项测试通过；合成测试不是数据验收。

- 修复了收盘后、EOD数据可用前生效事件可能被次日复核跳过的问题。状态基准新增 `status_asof`，固定指向selection_date收盘；`information_available_at`继续独立记录可用时间。此窗口事件不修改收盘状态，在09:25复核中应用。
- 空白、NaN或非文本证据不得证明PIT状态、股本覆盖、日历或Gate条件。
- 正式对照还要求输入表的实际日期匹配alignment证据且研究池质量为complete，不能复用其他日期的证明。

## Fact / Unknown：尚未解决的数据源限制

已核查的免费/现有来源尚不能证明完整历史全A股universe和逐日实际股本。TickFlow instruments不是历史股本接口；巨潮样本含定期报告及季度累计转股，不能直接逐日回填。沪深退市接口本身不是完整历史股票池。

[Tushare每日指标](https://tushare.pro/document/2?doc_id=32)和[聚宽数据说明](https://www.joinquant.com/help/data/stock?f=home&m=footer)提供相关历史接口，但本轮此前检查时环境未配置对应权限，用户确认没有新增数据权限。这里只列为待验证候选源，未证明其满足全部PIT要求，也没有购买、注册或请求用户降低标准。

数据生成阶段停在Gate 1A，1A/1B均未实际通过。恢复条件是取得可验证的数据与时点语义证据，完成全市场完整性核对和至少20只实际股本变动对照。期间不生成无验证历史，不前填季度末股本，不将当前名单当作历史universe。

尚未完成：完整市场生命周期及全量ST/停牌台账、20只真实股本验证、BK1158起始日和生效机制实证、真实日期对齐、Gate通过后的历史排名/导出/研究展示。这些交付项保持未完成，不以代码框架或测试通过替代。

# Checkpoint 6：用户点名事件的原始公告验证

以下生效日期为Fact，已从巨潮官方原始PDF逐一读取。公告、API响应、文本提取和SHA256另存在 `output/microcap_event_evidence_20260908/`；机器可读样本为 `verified_event_samples.json`。没有修改正式快照或旧产物。

| 证券 | 停牌起始日 | 复牌日 | 风险警示起始日及类型 | 原始公告 |
|---|---|---|---|---|
| 中设股份002883 | 2026-04-30 | 2026-05-06 | 2026-05-06，*ST | [2026-016](https://static.cninfo.com.cn/finalpage/2026-04-30/1225266057.PDF) |
| 如意集团002193 | 2026-04-30 | 2026-05-06 | 2026-05-06，ST | [2026-014](https://static.cninfo.com.cn/finalpage/2026-04-30/1225263624.PDF) |
| 美芝股份002856 | 2026-04-29 | 2026-04-30 | 2026-04-30，退市风险警示及其他风险警示 | [2026-027](https://static.cninfo.com.cn/finalpage/2026-04-29/1225248771.PDF) |
| 莫高股份600543 | 2026-04-24 | 2026-04-27 | 2026-04-27，*ST | [临2026-15](https://static.cninfo.com.cn/finalpage/2026-04-24/1225170075.PDF) |
| 龙韵股份603729 | 2026-04-29 | 2026-04-30 | 2026-04-30，ST | [临2026-023](https://static.cninfo.com.cn/finalpage/2026-04-29/1225247965.PDF) |
| 麦趣尔002719 | 2026-04-28 | 2026-04-29 | 2026-04-29，ST | [2026-036](https://static.cninfo.com.cn/finalpage/2026-04-28/1225222777.PDF) |
| 卓然股份688121 | 2026-05-06 | 2026-07-07 | 2026-07-07，*ST | [2026-032](https://static.cninfo.com.cn/finalpage/2026-07-06/1225410291.PDF) |

Fact：旧历史扩展脚本把美芝及麦趣尔的生效日都写成5月6日，与公告不符。旧脚本已经禁用，本次以独立证据样本纠正，不恢复旧硬编码选股路径。

Unknown：精确历史公开时间、其他事件及摘帽历史未齐备。API公告时间在样本中呈日期粒度，不能未经验证按午夜精确可知处理。所有样本的information_available_at保持null、pit_approved=false；卓然7月公告对5月停牌的回述只证明历史事实，不能提前用作5月决策证据。

本轮查询还发现仅按公司名称加“风险警示”搜索会漏掉生效公告并命中“可能被实施”的提示公告。补查使用证券代码和机构ID、遍历结果页，并按正文核验事件。七只样本不替代全市场universe或全量事件台账，不代表Gate 1A通过。

复核时发现运行时快照现已包含2026-09-08的400行，其SHA256变为`f1b117dca64eb6ac217d1c0f0c31154c56e591073e24276dfc11534d1d7213e3`。本轮未写该文件，不能继续声称它与早期checkpoint哈希相同；具体写入来源未核验。重新逐日比对后，原六个日期仍与旧重建完全匹配。七份PDF哈希和样本Unknown可用时间检查通过，diff检查通过。

# Checkpoint 7：按用户授权交付固定候选池估算 — 2026-09-09

用户明确接受以2026-06-22至09-08的快照代码并集回推，先做到2026-01-01，重点去掉历史ST和停牌。本阶段交付`microcap_fixed_union_estimate`，不冒充`BK1158_observed`，不将简化估算认证为严格Gate 1A通过。旧写入正式快照的脚本仍禁用。

## Fact：已运行产物

- 候选并集542只；BaoStock未复权日线89972行，覆盖2026-01-05至2026-09-08共166个交易日。1月1日至4日无交易，首日为1月5日。
- 全部542只请求成功，每个交易日返回542行；每日日线含`isST`、`tradestatus`、成交量。166日均输出20只，共3320条排名记录。
- 过滤依据为当日日线`isST=0`、`tradestatus=1`、成交量大于0、价格和股本锚点有效；没有将当前ST名称套用于全年。
- 候选日线中1911条ST证券日记录、215条停牌证券日记录被排除（两类可能重叠，不相加当作独立数量）。卓然5月6日至7月6日的43个停牌交易日，以及7月7日起46个ST交易日均被排除；此前允许入选。
- 恒尚节能603137的快照缺少价格，用同日BaoStock未复权收盘价和快照总市值恢复估算股本锚点，没有丢掉该候选股。
- 保留所有原始市值排名、策略排名、过滤原因、日线状态、股本锚点日期及下一交易日日期。下一交易日日期只是最早使用日，不代表已完成开盘前资格复核。
- 输出位于`output/microcap_union_estimate_20260101_20260908/`：`daily_top20.csv`、`daily_summary.csv`、`all_candidate_days.csv`、`candidate_anchors.csv`、`report.json`及542个独立日线CSV。
- 页面新增“历史估算（固定候选池）”标签，可查看趋势、逐日名单并下载CSV；只读取本地研究产物。
- 正式快照SHA256前后保持`f1b117dca64eb6ac217d1c0f0c31154c56e591073e24276dfc11534d1d7213e3`。

## Assumption / Inference / Unknown

- Assumption：使用每只股票首个可用快照的市值/价格估算股本，固定回推至年初。它不是逐日历史实际股本，期间送转、增发、回购可能影响市值和排名。
- Inference：这套名单可用于固定候选池的探索研究；后期快照并集会遗漏没有进入该并集的历史小市值股票，具有前视及幸存者偏差，无法据此声称复原真实BK1158。
- Unknown：历史精确信息可用时刻、股本变化修正、所有状态与公告逐一对应、BK1158历史规则仍未验证。严格Gate 1A/1B保持Unknown，不阻塞本次明确授权的估算交付。

## 复现

```bash
.venv/bin/python -m pip install -r scripts/requirements-microcap-research.txt
.venv/bin/python scripts/build_microcap_union_estimate.py --output output/microcap_union_estimate_20260101_20260908
```

请求参数与输入哈希匹配时复用独立目录中的已下载日线；参数或快照哈希不同则拒绝复用，应指定新研究目录。无修改正式快照、提交或推送。

## 本轮验证结果

- 43项专项unittest通过；`compileall app.py core services pages`及新增研究脚本/组件编译通过；`git diff --check`通过。
- 实际3320条Top20记录逐条检查：`isST=0`、`tradestatus=1`、成交量大于0、未复权标记3、市值为正、日期证券无重复、最早使用日期晚于选股日期；全部通过。
- Streamlit AppTest加载新研究组件无异常，并切换至2026-01-05确认显示20行名单。


# Checkpoint 8：补回戴帽前的六只股票 — 2026-09-09

Fact：上一版候选池仅为后期快照并集，漏掉此前用户点名的六只ST股票；逐日ST过滤不能弥补候选池遗漏。已补入中设002883、如意002193、麦趣尔002719、莫高600543、美芝002856、龙韵603729；卓然688121原已在池内。候选池增至548只。

六只补充股票使用BaoStock的2025三季报totalShare作固定股本估算，记录财报期末日期、公告日期和原始接口输出；不称其为逐日实际股本。所有补充日线均取得，估算输出90968条候选证券日、166日Top20共3320条；无请求失败、无缺股本锚点。旧v1产物保留，新产物及页面使用`output/microcap_union_estimate_20260101_20260908_v2/`。

已实际逐条核验：六只及卓然在公告停牌区间、戴帽起始日之后不出现在Top20中；戴帽前可正常入选。补充六只的最后可入选日及Top20入选次数：

|股票|最后可入选日|本次Top20入选交易日数|
|---|---|---|
|中设|2026-04-29|76|
|如意|2026-04-29|76|
|美芝|2026-04-28|49|
|莫高|2026-04-23|72|
|龙韵|2026-04-28|62|
|麦趣尔|2026-04-27|74|

共76个交易日的Top20名单相对v1发生变化，2026-01-05六只均入选。全部3320条名单记录的ST、停牌、成交量及每日20只断言通过。原始快照哈希保持不变。仅补充明确点名的股票，未声称历史候选池已穷尽。

复现命令：

```bash
.venv/bin/python scripts/build_microcap_union_estimate.py --output output/microcap_union_estimate_20260101_20260908_v2 --reuse-bars output/microcap_union_estimate_20260101_20260908
```


# Checkpoint 9：第200名与存量快照扩展曲线 — 2026-09-09

按用户授权，使用现有548只候选池及已下载日线生成`chart_metrics.csv`和`daily_top200.csv`。覆盖2026-01-05至09-08的166个交易日，每天都有微盘20均值和过滤后第200名市值，共33200条Top200名单。排名沿用当日ST/停牌过滤与固定股本估算，未重新请求行情。

“存量成分快照”默认勾选“补充年初以来历史估算（固定候选池）”：估算虚线、快照实线；重合日期优先使用存量快照（含此前已披露的旧重建可疑日期），不覆盖原始记录。取消勾选恢复仅57个快照日。图表及下载表保留来源标记，并提示候选池偏差、固定股本及衔接跳变。历史估算页另可下载全部Top200及估算曲线数据。

验证：12项针对性测试通过；全页面AppTest在屏蔽数据库初始化和网络请求条件下通过，开启扩展为166日期/4条市值轨迹，关闭为57日期/2条轨迹。实际数据每日200只及ST/停牌过滤断言通过；原始快照SHA256不变。项目编译及diff检查通过。
