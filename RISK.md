# RISK.md — 风险管理

> 配套文档：[README](README.md) · [ARCHITECTURE](ARCHITECTURE.md) · [STRATEGY](STRATEGY.md) · [BACKTEST](BACKTEST.md) · [RESEARCH](RESEARCH.md) · [实战手册（28 章）](docs/handbook/README.md)

本系统的风险管理不是单一模块，而是五层结构性约束 + 一份诚实的缺陷清单。

## 1. 五层风险防线

### 1.1 仓位层：策略即风控

- MA 带状择时天然的风险预算：空仓即零暴露；**全部 14 只参数化 ETF 的策略最大回撤
  均显著浅于持有基准**（最差也有 512890 示意口径的 -17.14% vs -16.53% 持平，
  最好如 588000 -26.62% vs -59.64%）；
- 半仓策略（159501/159655）给 50% 下限暴露，代价是单边牛市跑输持有
  （159501 近一年 +27.62% vs +33.08%）；
- 512890 停车把空仓现金转入低波动红利资产（风险从股市 beta 降为红利低波 beta）。

### 1.2 数据层：错误数据进不了决策链

- **三档时间语义**：只有 `latest_settled_trade_date`（收盘 +10 分钟）允许把今天写入
  正式日线；`filter_completed_market_dates` 在每次 append 前物理剔除盘中当日行；
- **append-only**：已缓存日期不可改写；修正走独立叠加层；
- **append-only 之上的复核**：`verify_updated_index_data` 用独立源（TickFlow 配置源/
  Yahoo/恒生官网）交叉验证目标日收盘价，容差 **±0.20%**，只读不覆盖；
- **复权完整性**：前加复权重叠变化 → 全量重建；重建失败 → `formal_history_valid=False`
  → 该标的信号、指引、512890 承接、50 万模拟**全部静默降级**（"不确定时拒绝说话"）；
- 基金缓存版本化（`fund_close_v2_*`）+ 行级元数据校验，禁止旧口径回退。

### 1.3 执行层：偏差有界

- 交易提醒先卖后买、100 股整手、净变化合并（袖套反向交易不重复下单）；
- 预判的七类前置失败直接拒绝生成行动清单（宁缺毋滥）；
- 期货手工录入强校验：时间晚于月末、平仓不超可用（事务式回删）、乘数无法确认拒录。

### 1.4 监控层：异常找得到人

- 铁矿石价格告警：边沿触发（首次跌破 730 元/吨推送，持续低于不重复，回升重新布防），
  `--test-price` 用临时状态文件零污染；
- 交易提醒：5 个槽位双通道（ServerChan + Hermes）并行，单通道失败不阻塞；
  **预判引擎报错时只发"提醒失败"，不发买卖建议**；
- 通知引擎：AUTO 通道回退链 ServerChan → PushPlus → WeCom，HIGH 以上自动加 Windows
  Toast；全部发送写 SQLite 历史。

### 1.5 元风险层：策略自己不可信时系统先停

- 冻结参数快照：只创建一次、内容不同即 `FileExistsError`、sha256、
  强制 freeze = 数据截止 +1 天；
- 真正样本外跟踪：`out_of_sample_daily.csv` 只追加冻结日后日期；
  无样本外时显示"尚无真正样本外交易日，不得计算或展示虚假的样本外绩效"；
- 参数孤峰检测：邻域退化 >5pp 标 `suspected_parameter_spike`；
- 回测执行假设的强制披露（after_close 的历史合法性声明）。

## 2. 已知风险与缺陷（分级）

### P0 正确性（详见手册第 28 章）

| # | 位置 | 问题 |
|---|---|---|
| 1 | `futures_live_settlements.py` | 新浪期权链反推只查看跌列，**call 期权结算价取不到** → 盯市估值缺失 |
| 2 | `futures_live_repository.py` | 同月双结单可产生孤儿数据集（账户/持仓口径错乱风险） |
| 3 | `position_timing.py` | 512890 承接行用 5 只全集、快照/回测用 3 只活跃集 → 指引与绩效口径分裂（当前配置下不触发，588000/159915 权重为 0） |
| 4 | `futures_live_daily_pnl.py` | 乘数缺失的成交连手续费也不计 → 费用低估 |
| 5 | `index_sources_yahoo.py` | 两处 `fromtimestamp` 无 tz，跨时区部署日期错位 |

### P1 一致性/可用性

当日收益分母双口径（正式层含当日买入成本 vs 实时叠加层昨收×数量）；
指数展示与回测在零偏离点等号归属相反；`超额收益(%)` 实为百分点差；
夏普两版无风险利率不可横向比较；WindowsToast 转义不全 / PushPlus 用 http /
ntfy 公网无鉴权；动态阈值研究 portable artifact 硬编码结论文案；
微盘股分页中断静默返回部分数据；相关性自动补算重复累积。

### 平台风险

- 守护脚本顶层 `import fcntl`：仅 WSL 可跑；`wsl.exe` 桥接断链时计划任务静默失败
  （只有任务计划程序的最后运行结果可查）；
- `core.cache` 的 Windows 占位锁无跨进程保护（单进程部署可接受）；
- `datasets.file_path` 绝对路径：跨机迁移后缓存"失明"（数据无损，需重建元数据）；
- SQLite 无 WAL：多进程并发写偶发锁等待；
- 静态假期表仅 2026 年：2027 年起依赖 exchange_calendars 可用或人工更新；
- 进程级行情缓存在多 worker 部署下各进程独立（午间单次闸失效）。

### 策略风险（诚实声明）

- 冻结参数来自历史研究，邻域稳健性检验不能消除"恰好好看"的可能；样本外跟踪
  截至 2026-09-03 仍不足一年；
- after_close 的 2026-07-06 前历史属当前规则模拟；模拟无滑点，实盘市价单有冲击成本；
- 512890 停车不是无风险收益：红利低波自身回撤（示意口径 MDD -16.53%）会进入账户；
- 收益率分母"出金不减"的口径会系统性放大大额出金后的收益率（有意设计，解读需知）；
- 单标的回测不含持仓期分红现金流（前加复权输入下收益正确、现金分红未入账），
  年度组合与审计引擎有完整处理——比较两套数字时注意口径。

## 3. 故障演练结论（真实验证）

- **引擎重构**：统一库重构前后全部回测数字逐字节一致（BACKTEST.md §6）；
- **缓存跨平台**：WSL 读 Windows 路径元数据 → 缓存"失明"，包装 load_dataset
  直接读 CSV 恢复（手册第 2 章排错案例）——数据无损是 append-only + 原子写的红利；
- **调度中断**：15:10 任务被杀 → 16:10 第二轮 pending 判定自动发现缺口补齐
  （pending 不依赖上一轮状态）；两轮全挂时"任务与数据"页两段式人工更新兜底。

## 4. 使用约束清单（操作前必读）

1. 盘中报价（卡片/预览）永不写入日线历史；任何"看起来像正式数据"的盘中显示
   都带"实时来源/实时时间"列；
2. 15:05 前不把当日价当收盘；15:00-15:05 的"收盘待确认"是过渡态；
3. 期货盯市与收盘口径不可混用（趋势图/日历强制二选一）；
4. 删除月结单文件不会删除已导入数据（按 source_file 重导幂等）——要撤销一次导入
   需手工清理对应行；
5. SendKey/API Key 只从环境变量或用户目录读取，永不入库、永不入日志
   （异常只记 `type(exc).__name__`）。
